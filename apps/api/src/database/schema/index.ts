/**
 * Drizzle schema registry for the Developer Platform API.
 *
 * Domain tables are intentionally absent in this persistence foundation.
 * Future modules (Projects, later) export tables from this folder and
 * re-export them here. The migration pipeline is established without
 * committing to a product schema prematurely.
 */
export const schema = {};
